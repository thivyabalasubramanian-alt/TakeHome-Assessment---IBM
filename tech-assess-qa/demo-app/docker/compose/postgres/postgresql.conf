# PostgreSQL Configuration for Debezium CDC
# Logical replication is required for Change Data Capture

# WAL Configuration - REQUIRED for CDC
wal_level = logical
max_replication_slots = 4
max_wal_senders = 4
wal_keep_size = 512MB

# Listen on all interfaces (Docker container)
listen_addresses = '*'
